# Database workspace

Place migrations/schema/seed definitions here after the architecture is approved. Separate raw Kobo snapshots, normalized analytical records, DEC Finding-to-Action data and human readiness decisions. Never commit real secrets or uncontrolled exports of real pilot data.
